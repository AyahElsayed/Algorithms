var countChars = function(str){

    return str.length; // no of char.  operations
};

countChars("dsance");
countChars("walkrealyfast");

// how much more work would it take
// to get the length of 1 million
// char string?
